This is a page that consumes an API to display different employees to give a task or get removed using redux toolkit 
just clone the repository , npm i to install the dependencies and npm start to open the page on port 3000
